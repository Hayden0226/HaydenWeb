---
import BaseLayout from '../../layouts/BaseLayout.astro';
import AnimatedCard from '../../components/AnimatedCard';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const projects = await getCollection('projects');
  return projects
    .map((project) => ({
      params: { slug: project.slug },
      props: { project },
    }));
}

const { project } = Astro.props;
const { Content } = await project.render();
const base = import.meta.env.BASE_URL;
---

<BaseLayout title={`${project.data.title} - Projects`} description={project.data.description}>
  <article class="max-w-5xl mx-auto">
    <!-- Header -->
    <header class="mb-12">
      <AnimatedCard client:load delay={0}>
        <div class="mb-6">
          <h1 class="text-4xl md:text-5xl font-bold mb-4" style="color: var(--text-primary);">
            {project.data.title}
          </h1>
          <p class="text-lg md:text-xl leading-relaxed" style="color: var(--text-secondary);">
            {project.data.description}
          </p>
        </div>

        <!-- Technologies -->
        {project.data.technologies && project.data.technologies.length > 0 && (
          <div class="mb-6">
            <h3 class="text-xs font-bold uppercase tracking-wider mb-3" style="color: var(--text-secondary);">Technologies</h3>
            <div class="flex flex-wrap gap-2">
              {project.data.technologies.map((tech) => (
                <span class="px-3 py-1.5 rounded-md text-sm font-medium shadow-sm" style="background-color: var(--accent); color: var(--accent-fg);">
                  {tech}
                </span>
              ))}
            </div>
          </div>
        )}

        <!-- Action Links -->
        {project.data.links && (
          <div class="flex flex-wrap gap-3 mb-6">
            {project.data.links.github && (
              <a
                href={project.data.links.github}
                target="_blank"
                rel="noopener noreferrer"
                class="action-button-primary inline-flex items-center gap-2 px-5 py-2.5 rounded-lg transition-colors font-medium text-sm"
              >
                <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
                </svg>
                View on GitHub
              </a>
            )}
            {project.data.links.demo && (
              <a
                href={project.data.links.demo}
                target="_blank"
                rel="noopener noreferrer"
                class="action-button-primary inline-flex items-center gap-2 px-5 py-2.5 rounded-lg transition-colors font-medium text-sm"
              >
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z"/>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
                Watch Demo
              </a>
            )}
            {project.data.links.website && (
              <a
                href={project.data.links.website}
                target="_blank"
                rel="noopener noreferrer"
                class="action-button-primary inline-flex items-center gap-2 px-5 py-2.5 rounded-lg transition-colors font-medium text-sm"
              >
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9"/>
                </svg>
                Visit Website
              </a>
            )}
          </div>
        )}

        <!-- Timeline -->
        <div class="flex items-center gap-2 text-sm" style="color: var(--text-secondary);">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
          </svg>
          <time datetime={project.data.startDate.toISOString()}>
            {project.data.startDate.toLocaleDateString('en-US', { year: 'numeric', month: 'long' })}
          </time>
          {project.data.endDate && (
            <>
              {' - '}
              <time datetime={project.data.endDate.toISOString()}>
                {project.data.endDate.toLocaleDateString('en-US', { year: 'numeric', month: 'long' })}
              </time>
            </>
          )}
        </div>
      </AnimatedCard>
    </header>

    <!-- Content -->
    <AnimatedCard client:load delay={0.1}>
      <div class="project-content">
        <Content />
      </div>
    </AnimatedCard>

    <!-- Footer -->
    <footer class="mt-12">
      <a
        href={base + "/work"}
        class="inline-flex items-center gap-2 transition-colors font-medium"
        class="accent-link"
        style="color: var(--accent);"
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
        </svg>
        Back to all work
      </a>
    </footer>
  </article>
</BaseLayout>
